import React from 'react';
import { REMOVE_FROM_WISHLIST } from '../Constants';

const RemoveFavourites = () => {
	return (
			<span className='mr-2'>{REMOVE_FROM_WISHLIST}</span>
	);
};

export default RemoveFavourites;
